import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  input: {
    backgroundColor: "white",
    color: "black",
    borderWidth: 1,
    borderColor: "#ddd",
    padding: 12,
    borderRadius: 8,
    marginVertical: 8
  }
});